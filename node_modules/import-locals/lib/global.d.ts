declare global {
    namespace NodeJS {
        interface Global {
            locals: GlobalPatcher;
        }
    }
}
export default class GlobalPatcher {
    separator: string;
    compile: Function;
    private exports;
    export(filename: string, variable: string, name: string): void;
    unexport(filename: string, variable: string, name: string): void;
    patch(): boolean;
    unpatch(): void;
    source(filename: string): string;
    private hasVariable;
    private getVariableIndex;
    private getVariables;
}
